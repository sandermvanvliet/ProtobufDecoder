name: release

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Set path for candle and light
      run: echo "C:\Program Files (x86)\WiX Toolset v3.11\bin" >> $GITHUB_PATH
      shell: bash
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore -c Release
    # - name: Test
    #   run: dotnet test --no-build --verbosity normal -c Release
    - name: Build MSI
      run: dotnet msbuild .\packaging\ProtobufDecoder.Installer\ProtobufDecoder.Installer.wixproj
    # - name: Create Release
    #   id: create_release
    #   uses: actions/create-release@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     tag_name: 1.0.${{ github.run_number }}
    #     release_name: 1.0.${{ github.run_number }}
    #     draft: false
    #     prerelease: false
    # - name: Upload Installer
    #   id: upload-release-asset 
    #   uses: actions/upload-release-asset@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps 
    #     asset_path: .\packaging\ProtobufDecoder.Installer\bin\Release\ProtobufDecoder.msi
    #     asset_name: ProtobufDecoder_v1.0.${{ github.run_number }}.msi
    #     asset_content_type: application/x-msi